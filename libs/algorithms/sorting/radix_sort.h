#ifndef NEW_PROJECTS_RADIX_SORT_H
#define NEW_PROJECTS_RADIX_SORT_H

#include <stdio.h>
#include <memory.h>
#include <stdlib.h>

void getPrefixSums(int *a, size_t size);

void radixSort(int *a, size_t size);

#endif //NEW_PROJECTS_RADIX_SORT_H
